# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ hhvm ]
  pull_request:
    branches: [ hhvm ]
    
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2
    
    - name: Confirm the expected cli
      run: |
        set -x
        hhvm -m daemon -p 8080
        sleep 5
        ps
        cat <<EOT >hello.hack
        <<__EntryPoint>>
        function main(): void {
        echo "Hello World!\n";
        }
        EOT
        curl http://localhost:8080/hellox.hack 
        #code=$(curl -sL -w "%{http_code} \\n" http://localhost:8080/hellox.hack -o /dev/null)
        #test $code = 404 || (echo "wrong URL returns $code, expected 404";exit 1)
        curl http://localhost:8080/hello.hack 
        #code=$(curl -sL -w "%{http_code} \\n" http://localhost:8080/hello.hack -o /dev/null)
        #test $code = 200 || (echo "correct URL returns $code, expected 200";exit 1)

